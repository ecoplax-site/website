/**
 * Ancla del contenido principal. La comparten el enlace de salto del layout
 * (src/app/layout.tsx) y el <main> de cada página, para que el destino no
 * dependa de que las dos partes escriban la misma cadena a mano.
 */
export const MAIN_CONTENT_ID = "contenido";
